import { HealthStub } from '../components/HealthStub';

// M14.6 — Запись на приём (Фаза A: заглушка). Дизайн — SOS24 Design.html секция m14, наполнение по docs/HEALTH.md.
export function HealthBookingScreen() {
  return <HealthStub code="M14.6" title="Запись на приём" subtitle="Формат приёма, дата и время, оплата" />;
}
