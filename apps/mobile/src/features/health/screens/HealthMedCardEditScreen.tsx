import { HealthStub } from '../components/HealthStub';

// M14.10 — Редактирование мед.карты (Фаза A: заглушка). Дизайн — SOS24 Design.html секция m14, наполнение по docs/HEALTH.md.
export function HealthMedCardEditScreen() {
  return <HealthStub code="M14.10" title="Редактирование мед.карты" subtitle="Форма всех полей мед.карты" />;
}
