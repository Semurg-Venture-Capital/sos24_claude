import { HealthStub } from '../components/HealthStub';

// M14.4 — Врачи и клиники (Фаза A: заглушка). Дизайн — SOS24 Design.html секция m14, наполнение по docs/HEALTH.md.
export function HealthDoctorsScreen() {
  return <HealthStub code="M14.4" title="Врачи и клиники" subtitle="Список, поиск, фильтры по специальности" />;
}
